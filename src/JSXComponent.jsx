import './index.css';

const JSXComponent = () => {
  return (
    <div className="jnsbfkjv">
      <h1
        style={{
          color: 'Red',
        }}
      >
        Siva
      </h1>
      <h1 className="blueColor">Chaitra</h1>
      <img src="image.png" />
      <br />
      <input type="text" onChange={() => {}} />
    </div>
  );
};

export default JSXComponent;

/**
 *
- Your JSX should return one parent only ? - Cleared
- All tags in JSX should be closed ? - cleared
- class/for should be written as className/htmlFor ? - Cleared
- html attributes ? should be written in camelCase ?
onclick -> onClick, onmouseover -> onMouseOver
- inline styles should be in object form ?
- Custom tag allowed with JSX ? 
 */
